import json

# Open and load the JSON file
with open("data.json", "r") as file:
    data = json.load(file)

# Print JSON data
print(json.dumps(data, indent=4))


import json

# Open and load the JSON file
with open("data.json", "r") as file:
    data = json.load(file)

# Print JSON data
print(json.dumps(data, indent=4))


import json

# Open and load the JSON file
with open("data.json", "r") as file:
    data = json.load(file)

# Print JSON data
print(json.dumps(data, indent=4))


