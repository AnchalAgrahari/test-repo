import  boto3
client = boto3.client(s3)

#for creating directory if not avsliable or present
bucket_name = input("Enter the bucket name: ").split()
folder_path = 'Documents/project-aws-tagger$'

if __name__ == '__main__':
    try:
        if len(bucket_name) != 0:
            print("Bucket name must unique and contain a-z,A-Z and '-' only",bucket_name)
        else:
            print("Bucket connot be created")

            


 
